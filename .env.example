# Sanity CMS — set these to enable the /studio admin and the live blog.
# Until they are set, the site renders the static sample articles in src/data/news.ts.
#
# Get these from https://www.sanity.io/manage after creating a project.
VITE_SANITY_PROJECT_ID=
VITE_SANITY_DATASET=production
